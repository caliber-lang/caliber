def add (x: Int, y: Int) -> Int =
    return x + y

def square (n: Int) -> Int =
    return n * n

def main =
    var a <- add(3, 4)
    var b <- square(5)
    print a
    print b
    print add(square(2), square(3))
